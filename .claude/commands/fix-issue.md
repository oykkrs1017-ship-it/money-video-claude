---
description: 動画コンポーネントの問題を調査・修正
argument-hint: [問題の説明]
---
## 問題修正: $ARGUMENTS

!`cd packages/tech-geopolitics-channel && npx tsc --noEmit 2>&1 | head -30`

以下の手順で対処:
1. 問題箇所を特定（エラーメッセージ or still画像で確認）
2. 該当コンポーネントを修正
3. TypeScriptコンパイルチェック
4. still画像で修正結果を確認
