[tool.uv.workspace]
members = [
    "packages/shared",
    "packages/trend-extractor",
    "packages/script-generator",
    "packages/script-reviewer",
    "packages/audio-synthesizer",
    "packages/uploader",
    "packages/pipeline",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-mock>=3.12",
    "ruff>=0.4",
    "mypy>=1.10",
]
